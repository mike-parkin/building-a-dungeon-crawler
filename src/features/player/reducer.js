
const intialState = {
    position: [0, 0],
}

const playerReducer = (state = intialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default playerReducer